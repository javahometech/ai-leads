@Library("app-shared-libs") _
         
pipeline {
    agent any

    stages {
        stage('Hello World') {
            steps {
               greet("Hari")
               add(10,30)
            }
        }
    }
}
